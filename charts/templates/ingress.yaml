apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catfact-ingress
spec:
  rules:
    - host: localhost          #{{ .Values.ingress.host }}
      http:
        paths:
        - path: /catfact
          pathType: Prefix
          backend:
            service:
              name: catfact-api
              port:
                number: 5000 #{{ .Values.service.port }}
